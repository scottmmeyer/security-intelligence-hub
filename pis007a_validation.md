# PIS-007A Validation

**Date:** 2026-06-15

---

## New Tests

```
tests/test_pis_007a_hardening.py

A. test_integrity_warning_emitted_when_positions_missing  PASSED
B. test_no_integrity_warning_when_positions_expected_zero PASSED
C. test_refresh_logging_started_and_completed              PASSED
D. test_refresh_logging_failure_message                    PASSED
E. test_lineage_summary_returns_latest_snapshot_date       PASSED

5 passed in 0.19s
```

## Test Scenarios

**A — Integrity warning: expected > 0, loaded = 0**  
Setup: Snapshot B has `position_count=5` in index but an empty `position_snapshots.csv`. Monkeypatches `_snapshot_groups` directly to avoid canonical pipeline.  
Verified: Warning contains `INTEGRITY_WARNING` and snapshot ID. No EXITED_POSITION records generated.

**B — No warning: expected = 0, loaded = 0**  
Setup: Both snapshots have `position_count=0` in index. Empty position files.  
Verified: `integrity_warnings` list is empty.

**C — Refresh logging: success path**  
Setup: Monkeypatches threading.Thread to run synchronously. `trigger_startup_refresh` succeeds.  
Verified: stderr contains `[PIS] Post-ingestion refresh started.` and `[PIS] Post-ingestion refresh completed.`

**D — Refresh logging: failure path**  
Setup: `trigger_startup_refresh` raises RuntimeError. Runs synchronously via patched Thread.  
Verified: stderr contains `started.` and `failed: test-induced failure`. Does NOT contain `completed.`. No exception propagated to caller.

**E — Dashboard date: latest snapshot_date over latest created_at**  
Setup: PAR-A has `snapshot_date=2026-06-14`, PAR-B has `snapshot_date=2026-05-01` but `created_at_utc` today.  
Verified: `latest_upload_date = "2026-06-14"`, `latest_par = "PAR-A"`.

---

## Regression Suite

```
tests/test_pis_006_post_ingestion_trigger.py   5 passed
tests/test_pis_performance_attribution_01.py  15 passed  
tests/test_pis_benchmark_attribution_01a.py    3 passed
tests/test_pis_benchmark_attribution_01b.py    5 passed
tests/test_pra_impl_02_funding_policy.py       10 passed

33 passed in 0.45s
```

---

## Live System Verification

### R1 — Change detection integrity

Current system state: all 68 snapshot partitions are intact, no integrity warnings generated by running `compute_all_snapshot_changes()` on the live data.

### R2 — Refresh logging

Post-ingestion trigger now emits:
```
[PIS] Post-ingestion refresh started.       ← new
[PIS] Startup refresh: all artifacts current ([canonical, ...]). Status: CURRENT
[PIS] Post-ingestion refresh completed.     ← new
```

### R3 — Dashboard date

```
Before:  latest_upload_date = 2026-05-29 (or CONCENTRATED_ALPHA)
After:   latest_upload_date = 2026-06-14  ✓
         latest_par         = PAR-20260614-3A8B91DB  ✓
```

---

## Files Modified

| File | Remediation |
|------|------------|
| `src/pis/change_detection.py` | R1: integrity check |
| `src/portfolio/runner.py` | R2: refresh logging |
| `src/pis/storage.py` | R3: sort key + CONCENTRATED_ALPHA filter |
| `tests/test_pis_007a_hardening.py` | Tests A-E (new) |
