# Python bytecode and caches
__pycache__/
*.pyc
*.pyo
*.pyd

# macOS filesystem metadata
.DS_Store

# Test and analysis caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/

# API keys and local secrets
.env

# Virtual environments
.venv/
venv/
env/

# Editor and OS temp artifacts
.vscode/
.idea/
*.swp
*.swo
*~

# Local temp scratch
*.tmp
*.temp
tmp/
temp/

# Runtime execution artifacts — pipeline run manifests, stage manifests, and log outputs.
# These are operational evidence produced at runtime, not architectural source.
# The scaffolding directories and their .gitkeep placeholders are tracked;
# all content produced during a run is gitignored.
runs/**
!runs/
!runs/.gitkeep
!runs/README.md
!runs/logs/
runs/logs/**
!runs/logs/.gitkeep
!runs/manifests/
runs/manifests/**
!runs/manifests/.gitkeep
!runs/manifests/example_run_manifest.json
!runs/stage_manifests/
runs/stage_manifests/**
!runs/stage_manifests/.gitkeep

# Runtime current-state outputs.
# Generated deterministically during each replay matrix build execution.
# Includes: analytical_universe.csv, replay_matrix.csv, replay_availability.csv,
# replay_inputs.csv, replay_performance_series.csv, current_snapshot_metadata.json.
# These are rebuildable at any time via build_wp05b_replay_matrix.py.
# Never source-controlled — consumers read from this path at runtime only.
# Local/generated runtime state
data/runtime/
data/current/

# Immutable historical replay and analytical universe artifacts.
# Includes: replay partitions (snapshot_date=.../replay_id=.../),
# analytical universe snapshots, snapshot registries (analytical_snapshot_registry.csv,
# replay_snapshot_registry.csv), market data partitions, and signal history.
# Historical analytical truth is retained locally only — never pushed to source control.
# The data/history/ root and benchmark contract scaffolding are tracked as anchors;
# all content produced at runtime is gitignored.
data/history/**
!data/history/
!data/history/.gitkeep
!data/history/benchmarks/
!data/history/benchmarks/benchmark_outcomes.csv
!data/history/benchmarks/benchmark_registry_history.csv
!data/history/benchmarks/benchmark_snapshots.csv

# Raw provider payload intake.
# ESS signal files ingested from external data providers are operational inputs,
# not architectural source. Lane placeholder directories are tracked; all
# provider payload files are gitignored.
incoming/ess/**
!incoming/ess/
!incoming/ess/non_starmine_zacks/
!incoming/ess/non_starmine_zacks/.gitkeep
!incoming/ess/starmine/
!incoming/ess/starmine/.gitkeep

# Fetched signal data from external providers (Zacks, Yahoo, Danelfin).
# These are runtime artifacts refreshed on demand — not source-controlled.
# The data/signals/ directory tree is rebuildable by running the fetch_* scripts.
data/signals/**
!data/signals/

# Supplemental seed data IS tracked (watchlist, portfolio vehicles).
# Only runtime signal fetches are excluded above.

# One-off debug and investigation residue.
# These are scratch files created during interactive debugging sessions.
# They are not governed build artifacts — governed diagnostics belong under
# explicitly named build_* scripts in scripts/diagnostics/.
final_verify.py
verify_state.py
process_report.py
status_output.txt
# Pattern covers any debug_*.py scripts created during future investigation sessions.
scripts/diagnostics/debug_*.py
# Root-level throwaway migration / scratch scripts.
migrate_zacks.py
script.py
solution.py
top5_analysis.py

# Scratch folder — debug artifacts from Phase 22D.2–22D.3 interactive sessions.
# macOS default "untitled folder" name; contents are transient debug captures.
untitled folder/

# Portfolio ingestion runtime artifacts.
# All PAR run outputs, normalized forms, archive CSVs, and the ingestion manifest
# are regenerated from the canonical CSV upload flow — not source-controlled.
# Scaffold directories are tracked; content is gitignored.
data/portfolio_ingestion/analysis_runs/
data/portfolio_ingestion/archive/
data/portfolio_ingestion/incoming/
data/portfolio_ingestion/normalized/
data/portfolio_ingestion/rejected/
data/portfolio_ingestion/manifest.json

# Incoming portfolio upload landing zone.
# User-uploaded portfolio CSV files are operational inputs, not architectural source.
incoming/portfolio/

# Allocation runtime artifacts.
# proposed/ and recalculation_snapshots/ are regeneratable outputs produced by the
# recalculation engine. manifest.json and evidence_history/ are append-only lineage
# records and are tracked alongside benchmark outcomes under data/history/.
data/allocation/proposed/
data/allocation/recalculation_snapshots/

# Classification audit runtime outputs.
# Timestamped audit JSON/TXT/CSV files are regeneratable via run_classification_audit.py.
data/classification_audit/

# Preserve deterministic test fixtures
!tests/fixtures/
!tests/fixtures/**

# UI session state (auto-updated on every navigation; not meaningful to track)
navigation_state.yaml

# Derived data outputs (regeneratable from signals + run history)
data/allocation/
data/derived/

# Temporary diagnostic and debug scripts (prefix convention: scripts/_*.py)
# Operational scripts are explicitly named without underscore prefix.
scripts/_*.py
!scripts/_run_intake.py

# Auto-generated audit reports (regeneratable from report-generator scripts)
# Exception: optimizer_ui_validation_report.md is a Phase 7.3B tracked deliverable.
archetype_validation_report.md
cash_deployment_report.md
cash_reconciliation_report.md
conviction_deployment_report.md
conviction_model_quality_report.md
conviction_ranking_report.md
coverage_denominator_report.md
coverage_gap_report.md
coverage_reconciliation_report.md
l1_allocation_gap_report.md
overlap_analysis_report.md
portfolio_philosophy_validation_report.md
portfolio_reconciliation_report.md
recommendation_conflict_report.md
recommendation_explainability_report.md
replay_alignment_audit.md
security_vs_etf_report.md
strategic_narrative_audit.md
strategic_narrative_validation_report.md
taxonomy_clean_run_report.md
taxonomy_reconciliation_report.md
ui_archetype_consistency_report.md

# Operator runtime state — live operator decisions, not architectural source.
# Committed default template: data/operator/portfolio_alignment_state.default.json
data/operator/portfolio_alignment_state.json
data/operator/cra_draft.json

# FMP data quality validation JSON — runtime artifact, regeneratable
data/analysis/fmp_dq_validation.json

# Analysis working artifacts and governance scratchpads
data/analysis/phase_*/
data/analysis/git_governance/

# PIS release-prep scratch artifacts
.pra_impl_*body.md
.pra_impl*.md
